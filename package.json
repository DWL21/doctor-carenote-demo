{
  "name": "doctor-demo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w @doctor/web",
    "dev:api": "npm run dev -w @doctor/api",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "validate:samples": "node scripts/validate-us-pdf-samples.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
